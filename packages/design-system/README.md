# design-system

Shared IBM Carbon MTSAi-specific tokens, base styles, and components.

This package is reserved for styles and components that need to be shared across
multiple apps in the monorepo. During early development, these files live directly
in `apps/Frontend`. Extract here when a second consumer (e.g. Backend admin UI) needs them.
